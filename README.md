## Setup

1. Clone the repository
2. Run `npm install`
3. Run `npm run setup` to create `.env` file
4. Add your Home Assistant credentials to `.env`:
   - Get token from Home Assistant → Profile → Long-lived access tokens
   - Add URL and token to `.env`
5. Run `npm start`